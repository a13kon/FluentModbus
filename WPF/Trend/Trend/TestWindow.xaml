<Window x:Class="Trend.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Trend"
        mc:Ignorable="d"
        Title="Стенд" Height="450" Width="800" Background="#26272B" Foreground="AliceBlue" >
    <Window.Resources>
        <Style  TargetType="{x:Type ScrollViewer}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar Grid.Column="1"
                       Opacity="0.5"
                       Name="PART_VerticalScrollBar"
                       Value="{TemplateBinding VerticalOffset}"
                       Maximum="{TemplateBinding ScrollableHeight}"
                       ViewportSize="{TemplateBinding ViewportHeight}"
                       Width="10"
                       MinWidth="10"
                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Background="#8A8E9C">
        <DockPanel LastChildFill="True">
            <Menu DockPanel.Dock="Top" Background="#31333A" Foreground="AliceBlue">
              
                <MenuItem Header="File">
                    <MenuItem Header="New Project"></MenuItem>
                    <MenuItem Header="Open Project">
                        <MenuItem Header="WinForms"></MenuItem>
                        <MenuItem Header="WPF"></MenuItem>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Exit"></MenuItem>
                </MenuItem>
                <MenuItem Header="Edit"></MenuItem>
                <MenuItem Header="View"></MenuItem>
            </Menu>
            <Grid>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="2.5*" MinWidth="150"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*" MinHeight="300"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*" MinHeight="150"></RowDefinition>
                </Grid.RowDefinitions>
                <GridSplitter Grid.Column="1" ShowsPreview="False" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#31333A"/>
                <GridSplitter Grid.Row="1" Grid.ColumnSpan="3" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#31333A"/>
                <ScrollViewer Grid.Column="2">
                    <StackPanel>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                        <Button MinHeight="60" Background="AliceBlue"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
